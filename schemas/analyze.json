{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dm2-tool/schemas/analyze.json",
  "title": "dm2 analyze --json Response Data",
  "description": "6W 分析 + 视图推荐的结构化输出",
  "type": "object",
  "properties": {
    "primary_6w": {
      "type": "string",
      "enum": ["What", "How", "Where", "Who", "When", "Why"],
      "description": "主要 6W 维度"
    },
    "secondary_6ws": {
      "type": "array",
      "items": { "type": "string", "enum": ["What", "How", "Where", "Who", "When", "Why"] },
      "description": "次要 6W 维度"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "分析置信度"
    },
    "extracted_entities": {
      "type": "object",
      "description": "提取的实体，键为实体类型，值为实体名称列表",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string" }
      }
    },
    "recommended_views": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "view_id": { "type": "string", "description": "视图 ID，如 OV-1" },
          "view_name": { "type": "string", "description": "视图名称" },
          "relevance_score": { "type": "number", "minimum": 0, "maximum": 1, "description": "相关度评分" },
          "reason": { "type": "string", "description": "推荐理由（含「路径完整性补充」等来源标注）" },
          "dm2_groups": { "type": "array", "items": { "type": "string" }, "description": "触发该推荐的 DM2 数据组" }
        },
        "required": ["view_id", "view_name", "relevance_score", "reason", "dm2_groups"]
      }
    },
    "suggested_views": {
      "type": "array",
      "items": { "type": "string" },
      "description": "建议的视图 ID 列表"
    }
  },
  "required": ["primary_6w", "secondary_6ws", "confidence", "extracted_entities", "recommended_views", "suggested_views"]
}
